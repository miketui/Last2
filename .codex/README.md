# .codex

Codex setup notes.

Primary Codex instructions live in root `AGENTS.md`.

Use:

```bash
bash setup.sh
```

Then run:

```bash
bash scripts/harness-preflight.sh
bash scripts/harness-validate.sh
```

For Codex cloud/cached environments, use `maintenance.sh` as the refresh script where supported.
