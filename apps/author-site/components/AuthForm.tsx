export function AuthForm({ mode }: { mode: "login" | "signup" }) { return <form className="mx-auto max-w-md rounded-3xl border border-whitegold/15 p-6"><label className="block text-sm font-semibold text-white" htmlFor="email">Email</label><input id="email" type="email" className="mt-2 w-full rounded-full px-4 py-3 text-obsidian" /><button className="mt-5 rounded-full bg-antique px-5 py-3 font-semibold text-obsidian">{mode === "login" ? "Log in" : "Create account"}</button><p className="mt-4 text-sm text-whitegold/70">Supabase Auth wiring scaffold. Configure env keys before production use.</p></form>; }
