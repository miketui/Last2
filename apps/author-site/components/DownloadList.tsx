import { deliverables } from "@/lib/downloads";
export function DownloadList() { return <div className="space-y-4">{Object.values(deliverables).map((item) => <form key={item.slug} action="/api/downloads/sign" method="post" className="rounded-2xl border border-whitegold/15 p-5"><input type="hidden" name="deliverable" value={item.slug} /><div className="flex items-center justify-between gap-4"><div><h2 className="font-display text-2xl text-white">{item.label}</h2><p className="text-sm text-whitegold/70">Private signed URL. Limit scaffold: 3 downloads / 7 days.</p></div><button className="rounded-full bg-antique px-4 py-2 text-sm font-semibold text-obsidian">Request secure link</button></div></form>)}</div>; }
