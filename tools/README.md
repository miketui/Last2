# Tools

Put helper tools here.

Examples:

```text
tools/epubcheck/
tools/pdf/
tools/screenshots/
tools/reports/
```

Do not commit large vendor binaries unless the repo intentionally tracks them.
Prefer setup scripts that install or download tools reproducibly.
